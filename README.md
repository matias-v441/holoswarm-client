## HOLOSWARM Client

Provides GUI for `iroc_bidge` service to:
- control individual UAVs
- create, run, and manage missions
- receive telemetry and mission feedback

## Install and run
In your python env (python>=3.11) run
```bash
cd holoswarm_client
pip install -e .
holoswarm-client --help
```
